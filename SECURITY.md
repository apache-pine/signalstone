# Security

Report vulnerabilities privately to the repository owner. Never include access tokens, Authorization headers, private message content, or files in reports.

Tokens remain in Obsidian Secret Storage. The HTTP client sends credentials only to the configured Webex API origin. Remote HTML is not injected into the DOM. Revoke and replace any token that may have been exposed.
